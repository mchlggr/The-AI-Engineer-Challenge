# HyperDX Telemetry
# For cloud HyperDX:
NEXT_PUBLIC_HYPERDX_API_KEY=your_hyperdx_api_key_here

# For local HyperDX (docker-compose up):
# NEXT_PUBLIC_HYPERDX_API_KEY=local
# NEXT_PUBLIC_HYPERDX_ENDPOINT=http://localhost:4318

# Local API dev proxy (Next.js -> FastAPI)
# In development, Next proxies `/api/*` to this target (default: http://127.0.0.1:8000).
# In production, proxying is disabled unless API_PROXY_TARGET is explicitly set.
API_PROXY_TARGET=http://127.0.0.1:8000
